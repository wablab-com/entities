{
  "attributes": {
    "group_name": {"type":  "string"},
    "status": {"type": "string", "description": "This will hold the group status like [opened, completed, canceled]"},
    "owner_id": {"type": "string", "description": "This will hold the owner user_id"},
    "is_private": {"type": "boolean", "description": "By default groups is publicly accessible"}
  }
}